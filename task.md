# Main task: lock in C++17 support

Make the remaining confirmed non-passing tests below pass with root `cpc.exe`, then close the remaining C++17 coverage gaps. Prioritize this work over unrelated development. Fix shared compiler/runtime behavior and retain deterministic regression coverage; do not weaken expectations or relabel valid tests as expected failures.

Continue adding independent coverage and reduce new failures individually. Keep batch-state recovery regressions passing when expanding the language implementation. The historical batch crash cascade is not an inventory of independent defects.

## Remaining confirmed failures

- Extend aggregate return evaluation through remaining indirect-call forms, nontrivial construction, mandatory copy-elision/self-reference lifetime, and union/bitfield/reference-field cases. Preserve test_constexpr_member_pointer_aggregate_return.cpp and test_constexpr_member_pointer_ambiguous_receiver.cpp for derived receiver adjustment, converted member-pointer copies, member return ABI and ambiguous-base rejection. Preserve test_constexpr_member_function_pointer_call.cpp and test_constexpr_member_pointer_runtime_call.cpp for known nonvirtual targets, receiver identity and runtime-call rejection; extend remaining inheritance/mixed virtual-base behavior, remaining aggregate-subobject assignment and comparison/null/virtual representation operations; preserve test_constexpr_member_pointer_assignment.cpp, test_constexpr_member_pointer_const_assignment.cpp and test_constexpr_member_pointer_unknown_assignment.cpp for evaluated target/adjustment writes, self/chained assignment, null reassignment, const mutation and unknown-source rejection; preserve test_constexpr_member_pointer_equality.cpp for target identity, null comparisons and composite-type receiver adjustments, remaining cast/access and ref qualification. Preserve test_constexpr_member_pointer_bool.cpp and test_constexpr_null_member_pointer_call.cpp for boolean conversion, null initialization/conversion and null-call rejection. Preserve test_member_pointer_private_base_conversion.cpp, test_member_pointer_private_base_static_cast.cpp, test_member_pointer_ambiguous_base_conversion.cpp, test_member_pointer_virtual_base_conversion.cpp and test_member_pointer_inaccessible_reinterpret_roundtrip.cpp for conversion constraints and named-cast operand context. Extend remaining C-style cv-combination and overload cases; preserve test_member_pointer_private_base_c_style_cast.cpp, test_member_pointer_ambiguous_c_style_cast.cpp and test_member_pointer_virtual_c_style_cast.cpp for access exceptions, nonzero adjustment, round trips and base-shape constraints. Preserve test_member_pointer_base_access_context.cpp, test_member_pointer_private_base_receiver.cpp and test_member_pointer_private_base_descendant.cpp for receiver access, lexical member/friend context and protected-base controls. Preserve test_nrvo_member_then_free_return.cpp and batch recovery; NRVO must use the function being analyzed, never stale function context. Preserve test_constexpr_indirect_aggregate_call.cpp and test_constexpr_indirect_runtime_aggregate.cpp for function-pointer parameter/reassignment behavior and discarded runtime-call rejection. Preserve test_constexpr_aggregate_return_arguments.cpp and test_constexpr_aggregate_return_parameter_pointer.cpp for free-function/operator results, ABI hidden-argument exclusion and pointer ownership. Preserve test_constexpr_range_for_class_sentinel.cpp, test_constexpr_aggregate_return_storage.cpp, test_constexpr_aggregate_return_local_pointer.cpp and test_constexpr_iterator_runtime_increment.cpp for result ownership, class iterator operations and discarded runtime-call rejection. Preserve test_constexpr_local_aggregate_copy_expression.cpp and test_constexpr_aggregate_copy_self_read.cpp for same-type copy initialization, independent storage, exactly-once initializer evaluation and self-read rejection.

- Preserve repaired test_chrono_clocks.cpp and test_template_static_factor_reference_argument.cpp; known integral constants must be read at value-consuming operations inside reference arguments. Extend remaining declaration/initialization ambiguity cases; preserve test_template_static_member_direct_argument.cpp, including qualified static-value construction and nested-type function declaration. Preserve repaired test_regex_ecmascript.cpp and test_template_static_flag_temporary_argument.cpp; nested value/default arguments must not inherit outer reference-fold suppression.

- Extend constexpr statement evaluation through class iterator/sentinel ranges, switch, nontrivial condition objects and remaining lifetime/performance cases. Preserve test_constexpr_range_for_members.cpp, test_constexpr_range_for_adl.cpp, test_range_for_member_names_block_adl.cpp and test_constexpr_range_for_temporary_escape.cpp for member/ADL lookup, ordinary-candidate exclusion, member-name precedence, range evaluation count and temporary expiry. Preserve test_constexpr_range_for_array.cpp and test_constexpr_range_for_value_escape.cpp for shared lowering, array reference mutation, value-copy iteration and lifetime rejection. Audit remaining range-for hidden-friend/template/access lookup, class sentinels, nontrivial temporary lifetime, and the existing nonstandard Size/index fallback. Complete auto deduction beyond the shared retained range-binding cases. Preserve test_constexpr_loop_declaration_condition.cpp, test_constexpr_while_condition_previous_iteration.cpp and test_constexpr_for_condition_dangling_read.cpp for per-iteration condition scope, increment lifetime, conversion-expression parsing and expired-object rejection. Preserve test_constexpr_if_declaration_condition.cpp and test_constexpr_if_condition_dangling_read.cpp for condition initialization, branch scope, reference binding and object expiry. Preserve test_constexpr_if_initializer.cpp and test_constexpr_if_initializer_dangling_read.cpp for initializer scope and expiry. Preserve test_constexpr_multiple_declarators.cpp and test_constexpr_later_declarator_self_read.cpp for base-type reset and declaration scope. Preserve test_constexpr_for_comma_clauses.cpp and test_constexpr_comma_condition_runtime_call.cpp for full-expression sequencing and discarded-call rejection. Preserve test_constexpr_for_mutation.cpp and test_constexpr_for_local_dangling_read.cpp for initializer scope, empty clauses and increment ordering. Preserve test_constexpr_do_while_mutation.cpp for initial body execution, continue/break condition ordering and early return. Preserve test_constexpr_while_mutation.cpp and test_constexpr_infinite_while.cpp, including per-iteration scope, break/continue/return propagation and bounded nontermination. Preserve test_constexpr_void_return_mutation.cpp and test_constexpr_void_runtime_call.cpp for empty return, fallthrough, void forwarding and runtime-call rejection. Preserve test_constexpr_conditional_return.cpp, test_constexpr_conditional_scope.cpp and test_constexpr_block_local_dangling_read.cpp; retain scope-aware return propagation and skipped side-effect isolation.

- Extend remaining scoped member and specialization cases. Preserve test_constexpr_scoped_member_receiver.cpp and test_constexpr_scoped_member_pointer_return.cpp, scoped definition parameter-name binding and duplicate lambda star-this capture rejection. Preserve repaired implicit-member lookup and pointer-prvalue receiver representation. Extend specialization and remaining definition/redeclaration cases. Preserve test_constexpr_scoped_member_const_parameter.cpp and the paired const-write rejection; retain definition parameter qualifiers independently of canonical function types.

- Audit remaining call-lowering paths for discarded runtime side effects during constant evaluation. Preserve test_constexpr_discarded_runtime_call.cpp, test_constexpr_discarded_nested_runtime_call.cpp and test_constexpr_discarded_call_contexts.cpp; failed evaluation must survive discarded values while unevaluated/skipped operands stay valid.

- Extend aggregate parameter evaluation to remaining static subobject sources and nontrivial copy/lifetime cases. Preserve test_constexpr_static_aggregate_argument.cpp and rejection of mutable global sources and static mutable-member reads. Preserve test_constexpr_aggregate_parameter_mutation.cpp, copied pointer-member aliasing and aggregate-parameter pointer escape rejection. Audit remaining parameter reconstruction and runtime qualifier paths; preserve static-subobject argument, redeclared const-parameter, member const-write rejection and writable member-parameter controls; preserve test_constexpr_const_value_parameter_write.cpp. Extend aggregate parameter objects and audit failed evaluation rollback and remaining parameter qualification/lifetime. Preserve test_constexpr_value_parameter_mutation.cpp, test_constexpr_parameter_object_identity.cpp and parameter-pointer escape rejection; retain implicit this receiver behavior. Preserve test_constexpr_skipped_mutation.cpp, nested mutable-member mutation of const evaluation-owned locals and inherited-const sibling write rejection. Preserve test_constexpr_nested_reference_mutation.cpp and test_constexpr_nested_pointer_mutation.cpp. Extend remaining aggregate mutation, lifetime and control-flow evaluation. Preserve test_constexpr_local_array_assignment.cpp, test_constexpr_nested_array_mutation.cpp and out-of-bounds array-store rejection. Preserve test_constexpr_local_member_assignment.cpp and cast-away-const member write rejection in test_constexpr_const_member_assignment.cpp. Extend remaining mutation and value-category paths. Preserve test_constexpr_local_increment.cpp and test_constexpr_parenthesized_local_assignment.cpp, including nested parentheses, assignment, reference binding and value return. Preserve test_constexpr_direct_increment_values.cpp and global increment rejection. Extend mutation through aggregate subobjects and remaining value-consumption paths. Preserve test_constexpr_local_compound_assignment.cpp, including arithmetic/shift/bitwise operations and chained assignment. Preserve scalar reference/pointer alias assignment, pointer reassignment, original const-object write rejection, global-write and discarded runtime-call controls. Preserve assignment-lvalue cleanup after diagnostics so failed evaluation cannot crash later batch items. Extend scalar object evaluation through mutation, remaining lifetime cases and aggregate copy/constructor cases. Preserve repaired test_constexpr_local_self_address.cpp, scalar pointer/reference reads, identity, uninitialized self-read and callee-local pointer escape rejection, and evaluator temporary-allocation state restoration. Preserve repaired test_constexpr_local_pointer_member_copy.cpp and test_constexpr_same_type_aggregate_list_copy.cpp, initializer-defined declarations, construction evaluation order and copied-pointer originating-array bounds rejection. Preserve repaired evaluation-owned pointer-member initialization in test_constexpr_aggregate_self_address.cpp, member/array pointer reads, originating-subarray bounds and pointer-escape rejection. Preserve passing array self-address identity and uninitialized array self-read rejection; keep real object identities and offsets.

- Preserve repaired runtime character-pack spelling and independent ordinary/punctuation/escaped character template values; audit remaining integer template-value widths. Extend evaluation-owned local aggregate initialization through remaining declaration-scope cases, self-address identity, mutation and nontrivial constructors. Preserve scalar/array self-read rejection when locals shadow globals, unevaluated sizeof self-uses and explicit global qualification. Preserve repaired test_constexpr_character_pack_array.cpp and test_numeric_literal_template_spelling.cpp, inferred/explicit bounds, zero fill, nested arrays, member reads and callee-local pointer escape rejection. Keep the spelling, exponent-sign and cooked/template-priority controls; pack counts alone do not prove values.

- Complete user-defined literal suffix recognition and dispatch beyond the retained cooked/raw/template numeric cases. Preserve literal-template character-pack dispatch, saved operator-name identity, ordinary trailing non-type pack linkage and raw/template conflict rejection; extend declaration restrictions and member-template pack linkage. Preserve raw source spelling and out-of-built-in-range numeric inputs, cooked-over-raw precedence and independent numeric grammar validation. Preserve 21_twice, exact integer/floating parameter selection, hexadecimal forms, separators and maximum unsigned 64-bit values. Cover cooked numeric, character/string, raw and template literal operators, permitted signatures/scope, namespace/using/visibility lookup, reserved suffixes, overload rules, overflow diagnostics and error-path resource ownership. Preserve explicit literal-operator name parsing, rejection of nonempty strings and direct constexpr/runtime calls; preserve macro/directive/line-number behavior during string-sequence lookahead.

- Extend digit-separator coverage to remaining malformed numeric literals, user-defined suffixes and language-mode rules. Preserve macro spelling, integer bases, fractional/exponent separators, unsigned/long-long suffixes and leading-point/negative-exponent controls. Preserve rejection beside base prefixes, decimal points, exponent markers and suffixes, and rejection of repeated apostrophes.

- Improve static_assert diagnostic source attribution: a failed assertion can report the following source line. Preserve rejection of direct/stringified false string-byte conjunctions and agreement with volatile runtime comparisons.

- Complete remaining encoded-literal rules: remaining mixed-prefix policies and concatenation edge cases, remaining raw/prefixed-literal lexical edge cases (including universal-character-name token boundaries), remaining narrow/wide numeric-escape rules, source encoding and encoding-specific diagnostics. Preserve ordinary/encoded concatenation in both orders, all supported encoding widths, macro/raw components, independent escape boundaries and UTF-8/wide rejection. Preserve inactive preprocessing-number identity through universal-character names, dotted suffixes, exponent signs, apostrophe separators and line splices. Preserve inactive raw-prefix recognition across input-buffer boundaries and line splices, with whitespace/comment/longer-identifier controls. Preserve active-token prefix recognition across input-buffer boundaries and line splices, including macro-name controls. Preserve raw delimiter matching/validation, literal backslashes and newlines, backslash-newline pairs, encoding prefixes, macro replay and inactive-branch directive isolation. Preserve UTF-8 string/character token replay, char/code-unit types, supplementary UTF-8 byte sequences, same-prefix concatenation and typed character template arguments; reject multi-unit UTF-8 characters. Preserve UTF-16/UTF-32 numeric-escape overflow rejection, maximum code units and the distinction between numeric escapes and universal character names. Preserve UTF-16/UTF-32 token replay, array/code-unit widths, same-prefix concatenation, supplementary Unicode strings, typed template character arguments and rejection of multi-unit UTF-16 characters/invalid Unicode scalars. Preserve wide-literal constant/runtime identity.

- Extend originating-array provenance through remaining stored pointer members/arrays, cast, reference and subobject paths. Preserve global constexpr pointer/alias bounds, one-past subtraction, null values, unevaluated operands and global pointer subarray-boundary rejection. Preserve pooled literal storage and agreement between constant/runtime identity, including unequal strings and embedded nulls; audit remaining encodings, predefined function-name objects and cross-translation-unit behavior. Preserve rejection in `test_constexpr_pointer_subarray_boundary.cpp`, direct and returned subarray boundary controls, and distinct-row subtraction rejection. Preserve local pointer offset reads, one-past address formation/subtraction for static and temporary arrays, and unevaluated out-of-range operands.

- Extend local object value tracking to array pointer arithmetic and address/lifetime rules, unions with active-member tracking, remaining reference forms created during constant evaluation, and nontrivial constexpr constructors; preserve direct/nested reads, zero-initialization, retained local and global-source trivial-copy cases, local array indexing (including nested dimensions and aggregate elements), global-source array-member copies, unevaluated array operands, mutable-member rejection, static-referent reference chains, automatic-referent constant-expression rejection, and runtime storage duration.

- Extend constexpr initializer enforcement beyond retained local scalar writes and global dynamic-fallback rejection to constructor-driven initialization, union copies, and remaining storage forms. Preserve mutable-subobject and non-constexpr-source rejection for named-object local/global constexpr copies, valid copies without scalar reads, and runtime mutable copies. Preserve rejection of ordinary const aggregate and volatile-reference member reads. Extend array reads to remaining aggregate elements and subarray/provenance bounds; preserve typed scalar static reads, signed-value interpretation, relocations, string-literal extents, ordinary-const rejection and whole-object bounds. Extend initialization-state tracking to constructor-driven paths; preserve rejection of local/static aggregate forward reads, acceptance of earlier initialized members, local default-member binding, and runtime/name-shadowing controls. Audit diagnostic recovery for objects under construction and remaining volatile array paths. Preserve evaluated local scalar direct/list/copy-list initialization, empty-list zero initialization, representable integer narrowing, exact integer-to-floating list conversions (including large powers of two), and rejection of out-of-range/floating-to-integer/inexact integer-to-floating list conversions. Complete remaining list conversion paths and exceptional floating-value/target-format coverage. Preserve rejection of runtime floating rank reduction and finite floating overflow, acceptance of rounded in-range constants, underflow to zero and widening, and ordinary scalar brace-list checking. Preserve explicit aggregate/array element narrowing checks and independent default-member initializer context with exact runtime call counts. Preserve `test_local_constexpr_scalar_initializers.cpp` and runtime-call rejection controls.



- Extend evaluation-owned temporaries beyond retained constant-expression member/reference reads and `if constexpr` receiver conversions: global aggregate temporary copies and remaining reference initializer forms, remaining constant-expression pointer sources, remaining temporary-pointer/provenance paths, remaining reference-return/binding forms and lifetime extension, mutation during evaluation, nontrivial constexpr constructors remain incomplete. Preserve mutable temporary reads (including nested members, arrays and receiver conversions) while rejecting mutable named-object reads. Preserve default-member initialization of evaluation-owned temporaries, nested receiver offsets, named-object runtime receiver addresses, ordinary runtime temporary/indirect-result initialization with exact call counts, and forward-read rejection. Preserve local constexpr copies of temporary subobjects, including mutable reads during evaluation. Preserve distinct simultaneous temporaries, nested reference calls, local/global scalar temporary initializers, initialized aggregate/array values, nonconstant-initializer rejection and ordinary dynamic initializer execution. Audit full-expression cleanup and diagnostic recovery across all entry points.

- Preserve rejection in `test_constexpr_temporary_address_escape.cpp`; its rejection does not establish complete lifetime/escape checking because reference lifetime extension and remaining pointer/provenance paths are incomplete. Preserve static reference-return identity, caller-owned temporary reference reads, callee-owned reference rejection and const qualification checks. Extend remaining reference-category and conversion rules; preserve mutable/volatile lvalue-reference temporary rejection and array lvalue/xvalue argument controls; preserve rejection of direct rvalue-reference binding to lvalues in evaluated declarations/returns and call arguments, valid temporary/xvalue bindings, member-pointer prvalue categories and rvalue category preservation when lowering static lifetime extension. Preserve evaluated local direct/list/copy-list reference initialization, trailing list commas, parenthesized comma expressions and rejection of multiple reference initializers. Preserve evaluated local pointer bindings/chains, null pointers, static member-address identity, caller-owned temporary reads and callee-owned pointer escape rejection. Preserve local reference bindings and chains, static referent identity, local aggregate-temporary reference reads, const qualification checks and rejection of callee-owned reference escapes. Preserve member-call constant receiver addresses, reference result identity, temporary member reference reads and runtime receiver call counts. Preserve immediate reads through pointers returned to caller-owned temporaries, rejection of callee-owned temporary pointers, and rejection of subtraction between distinct temporaries. Do not give evaluation temporaries static lifetime or substitute placeholder receiver values. Preserve member lvalues during address formation and global constexpr pointer relocation/null identity, typed member-pointer dereferences, pointer reads of constant-initialized complete const integral objects, runtime-initialized const-object rejection, rejection of cast-away-volatile static object reads, independent constant-expression folding contexts, mutable-member pointer rejection, and static pointer returns through constexpr calls and initializers. Preserve named-object reads, mutable named-member rejection, past-object rejection and runtime storage duration.

- Continue auditing constexpr contextual conversions, access/deleted-function checks, further placeholder return forms, and definition-time checking of discarded template statements. Preserve the repaired explicit-bool temporary receivers, condition declaration scope, and mutable-condition rejection.

## Remaining coverage before claiming C++17 support

- Complete structured-binding initializer forms, tuple member/ADL protocols, cv/reference identity, lifetime, access and invalid-declaration checks; extend the retained array and aggregate cases.
- Complete variable-template specialization declarations, namespace lookup, packs, conditional substitution, redeclaration checks and cross-translation-unit identity beyond the retained partial-specialization tests.
- Finish selection initializer scope/lifetime validation. Complete optional constexpr behavior, conditional special-member availability/triviality, API overloads, and exception contracts.
- Complete fold coverage: operand grammar, nested folds, constexpr identities/dereference, multiple and non-type packs, binary member-pointer folds, overloaded operators, larger folds, and the feature macro.
- Audit `if constexpr`, constexpr lambdas, CTAD, inline variables across translation units, noexcept function types, over-aligned allocation, sequencing, and attributes.
- Complete lambda constexpr behavior and closure special-member/aggregate rules. Extend noexcept coverage beyond the retained call/conversion, conditional generic, unevaluated-expression, allocation, RTTI, default-constructor/member-initializer, defaulted-copy boundary, local-class move boundary, elementwise defaulted-assignment and out-of-class definition cases: out-of-class defaulting still needs deleted-function, triviality, value-initialization, redeclaration and cross-translation-unit linkage checks; union variants, virtual-base cleanup, further local-class/overload/conversion paths, substitution recovery, and generic closure conversion also need auditing. Extend scope/cv/access, copy, move, reference, nested capture and backing-array lifetime coverage beyond the retained initializer forms, name-conflict/shadowing and `*this` tests. Audit advertised builtin/feature support against actual semantics and remove remaining name-based behavior overrides.
- Audit C++17 library facilities, including the recorded missing `any` and `variant` support; add minimal local lifetime, assignment, access, and exception-contract tests.
- Reduce distinct cases from inspected upstream inputs into `Tests/`, reuse existing equivalent regressions, and track unverified behaviors explicitly. Passing this failure list alone does not establish exhaustive C++17 conformance.
- Audit remaining negative-test diagnostics against their intended language rules; compiler-only rejection alone does not prove the diagnostic is for the intended defect.
- Complete deleted-function rules for destructors, implicitly deleted special members, and additional template, address-taking and redeclaration contexts. Audit constructibility traits for abstract classes, destructor availability, references, conversions, access, arrays, and substitution boundaries beyond the retained cases.

## Execution and acceptance

1. Work with one compiler process at a time and stable root `cpc.exe`; investigate CPC failures using CPC. Use no external compiler without explicit authorization.
2. Repair the remaining failures above. Run each exact reproducer and focused related tests after its fix.
3. Run `Tests/test.exe -Regression` and `Tests/test.exe -All -Tier fast` after the focused repairs. Publish compiler changes through `scripts/build.exe`; use `-RebuildRuntime` when an explicit runtime ABI/code-generation refresh is needed. Preserve root `cpc.exe` and `lib/` if publication fails.
4. After the substantial C++17 changes, run the relevant full suites and the pedantic gate serially to detect failures masked by the original crash. Record compiler identity and fresh evidence in `build/`, update the coverage data, and remove resolved work from this document.
5. Close the remaining coverage gaps before declaring C++17 support locked in; retain all valid positive tests and intended rejection tests with their original semantics.
