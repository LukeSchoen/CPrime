/* Shared standard headers for the warm-PCH comparison. Its construction time
   is recorded separately; changing these headers invalidates the cache. */
#include <vector>
#include <algorithm>
#include <cmath>
#include <cassert>
#include <cstddef>
#include <cstdint>
#include <limits>
