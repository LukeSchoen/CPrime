#ifndef _CPC_WTYPES_H
#define _CPC_WTYPES_H

/* The Windows SDK exposes the Automation base types through wtypes.h.
   CPrime's compact SDK only needs the ordinary Win32 definitions today, and
   windows.h is the compatible public umbrella for those declarations. */
#include <windows.h>

#endif
