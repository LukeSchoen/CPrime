# Windows command scripts must use CRLF so cmd.exe can reliably resolve labels.
*.cmd text eol=crlf
*.bat text eol=crlf

# The worker progress log is appended and read back by build/worker-status.exe;
# keep its bytes exactly as written so timestamps and columns stay stable.
Tests/progress/log.tsv -text
