# First-party pedantic cases that currently fail and count against the 100% target.
# One suite-relative path per line, matching the Tests/tiers.json key form.
# Delete the line when the case is repaired; keep this list in step with the
# "Known first-party pedantic failures" section of task.md.
# Empty since cycle 19: the internal baseline is green at both tiers. Cycle 19
# repaired the const range-for loop variable, the coroutine syntax body, the
# by-value parameter destruction expectation and the named return value
# optimization.
