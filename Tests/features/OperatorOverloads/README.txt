Operator overloads feature suite

Goal:
- Support C++-style member operator declarations and definitions for `+`, `-`, `*`, `/`, `>>`, and `<<`.
- Use expression syntax on class/struct values.

Usage:
- powershell -NoProfile -ExecutionPolicy Bypass -File Tests\\run.ps1 -Suite features/OperatorOverloads
